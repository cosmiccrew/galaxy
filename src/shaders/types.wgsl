#define_import_path galaxy::shader_types

struct CelestialSettings {
    seed: f32,
    pixels: f32,
    rotation: f32,
    radius: f32,
    time_speed: f32,
}

// struct Colour {
// 	colour: vec4<f32>
// }
struct Colour {
    colour: vec4<f32>
}
